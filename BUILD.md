# BUILD

```bash
cargo build --no-default-features
cargo build
cargo build --features rfcomm
cargo build --all-features
cargo test --no-default-features
```

`libdbus-1-dev` is required on Linux hosts that build BT crates and utilities.
